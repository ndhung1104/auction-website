# Frontend Testing

E2E test dependencies are isolated here to keep the main app lighter. Install and run tests from this folder:

```bash
cd testing/frontend
npm install
npm run test:e2e
```

Place Playwright specs alongside this package.json (e.g., `tests/**/*.spec.js`).***
